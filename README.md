The spider generates an infinite stream of URLs that have to be validated before continuing.

Issue: the parse(response) method is never called.

To run the parser call:
```bash
scrapy crawl streamspider
```
